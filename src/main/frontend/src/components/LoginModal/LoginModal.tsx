import { Modal } from 'antd';

const LoginModal = () => {
    const handleOk = () => {};

    return (
        <Modal
            title="Login"
            visible={false}
            onOk={handleOk}
            onCancel={() => {}}
        >
            gfds
        </Modal>
    );
};

export default LoginModal;
