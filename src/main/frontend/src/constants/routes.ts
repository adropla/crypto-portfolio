type typeRoutes = {
    main: string;
    portfolio: string;
    watchlist: string;
    coin: string;
    exchanges: string;
    settings: string;
};

const ROUTES: typeRoutes = {
    main: '/',
    portfolio: '/portfolio',
    watchlist: '/watchlist',
    coin: '/coin',
    exchanges: '/exchanges',
    settings: '/settings',
};

export default ROUTES;
